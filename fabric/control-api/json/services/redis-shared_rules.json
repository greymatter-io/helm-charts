{
    "zone_key": "{{ .Values.global.zone}}",
    "shared_rules_key": "{{.service.serviceName}}-{{ .Values.mesh_redis.name }}-rules",
    "name": "{{.service.serviceName}}-{{ .Values.mesh_redis.name }}",
    "default": {
      "light": [
        {
          "constraint_key": "",
          "cluster_key": "{{.service.serviceName}}-{{ .Values.mesh_redis.name }}-cluster",
          "metadata": null,
          "properties": null,
          "response_data": {},
          "weight": 1
        }
      ],
      "dark": null,
      "tap": null
    }
  }