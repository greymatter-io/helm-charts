{
    "zone_key": "{{ .Values.global.zone}}",
    "shared_rules_key": "edge-{{ .Values.mesh_redis.name }}-rules",
    "name": "edge-{{ .Values.mesh_redis.name }}",
    "default": {
      "light": [
        {
          "constraint_key": "",
          "cluster_key": "edge-{{ .Values.mesh_redis.name }}-cluster",
          "metadata": null,
          "properties": null,
          "response_data": {},
          "weight": 1
        }
      ],
      "dark": null,
      "tap": null
    }
  }